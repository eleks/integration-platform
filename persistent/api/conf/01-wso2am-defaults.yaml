---
wsdl_epr_prefix:
  http:
    prefix: http://${role.host}:${role.port.http}
    bind_address: 0.0.0.0
  https:
    prefix: https://${role.host}:${role.port.https}
    bind_address: 0.0.0.0
